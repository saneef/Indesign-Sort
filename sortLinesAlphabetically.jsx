main();function main(){    if ((app.documents.length != 0) && (app.selection.length != 0)) {        if (app.selection[0].constructor.name == "TextFrame"){            //Text frame is selected            myFrame = app.selection[0];            var arr = new Array();            for(var i = 0; i < myFrame.texts[0].paragraphs.length; i++){                arr.push(myFrame.texts[0].paragraphs[i].contents);            }            try{                myFrame.texts[0].remove();            } catch (e) {} // An error indicates the frame was already empty                        arr.sort();                        var finalString = "";                        //app.selection[0].paragraphs[0].insertionPoints[-1].contents  = "test";            for(var i = 0; i < arr.length; i++){                finalString = finalString+arr[i];            }            myFrame.contents  = finalString;        }else{                alert("Select a text frame and then run the script"); return false;        }    } else {        alert("Select a text frame and then run the script"); return false;    }}